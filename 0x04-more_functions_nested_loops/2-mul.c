#include "main.h"
/**
 * mul - a function multiply two numbers
 * @a: ASCII number accept
 * @b: ASCII number accept
 * Return: c print number
 */
int mul(int a, int b)
{
	int c = a * b;

	return (c);

}
