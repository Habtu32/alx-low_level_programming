#include "main.h"
/**
* _abs - Determines if the input number
*
* @t: The input number as an integer.
*
* Return: absolute value
*/
int _abs(int t)
{
	if (t >= 0)
		return (t);
	else
		return (-t);
}
