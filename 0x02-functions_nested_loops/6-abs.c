#include "main.h"
/**
 * _abs - to print absolute value
 *
 * @n: for accepting intiger
 *
 * Return: n if absolut
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (n * -1);
	}
}
